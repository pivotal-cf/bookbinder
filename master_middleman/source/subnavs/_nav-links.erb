<% unless links?(@links) %>
  <ul>
    <% @links.each do |link| %>
      <li class="<%= submenu_class(link) %>">
        <a href="<%= link[:url] %>"><%= link[:text] %></a>
        <%= render_links link[:nested_links] %>
      </li>
    <% end %>
  </ul>
<% end %>
